import { HighlightOnSearchPipe } from './highlight-on-search.pipe';

describe('HighlightOnSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightOnSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
